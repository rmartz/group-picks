// Superseded by InviteSection.spec.tsx via InviteSectionView.spec.tsx
// (this PR). File retained as a placeholder because the agent could not
// delete it; clean up in a follow-up.
import { describe, it } from "vitest";

describe.skip("InviteLinkSection (superseded)", () => {
  it("has no remaining tests", () => {
    // intentionally empty
  });
});
