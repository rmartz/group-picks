# Public environment variables for the production environment.
#
# Only non-sensitive variables belong here. All keys are validated against
# deployment/schema.yml — sensitive variables (private keys, auth tokens, DSNs)
# will be rejected by the pre-commit hook and CI.
#
# Sensitive variables are managed directly in Vercel and are never committed.
# Pulling a local .env.local from Vercel is handled by envctl (TBD).
#
# To update these public values, edit this file directly, then run `pnpm run env:validate`.

environment: production

variables:
  # Firebase Client SDK — safe to expose; access control is enforced by Firebase security rules.
  # NEXT_PUBLIC_FIREBASE_API_KEY is a public Firebase Web API key (not a secret), but its "AIza…"
  # shape trips GitHub secret scanning, so its value is set directly in Vercel and left blank here.
  # The remaining values are plain identifiers with no secret-pattern shape and stay committed.
  NEXT_PUBLIC_FIREBASE_API_KEY: ""
  NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: "group-picks.firebaseapp.com"
  NEXT_PUBLIC_FIREBASE_PROJECT_ID: "group-picks"
  NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: "group-picks.firebasestorage.app"
  NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: "602500582991"
  NEXT_PUBLIC_FIREBASE_APP_ID: "1:602500582991:web:1d46a0cf7e8ec3a31c243a"
  NEXT_PUBLIC_FIREBASE_DATABASE_URL: "https://group-picks-default-rtdb.firebaseio.com"

  # Firebase Admin SDK — project identifiers only, not credentials.
  # The service account itself (FIREBASE_CLIENT_EMAIL, FIREBASE_PRIVATE_KEY) is
  # a Vercel-managed secret and is never committed here (the schema denies
  # FIREBASE_CLIENT_EMAIL). FIREBASE_SA_EMAIL used to duplicate the SA email
  # under a non-denied alias but was read by no code, so it has been removed.
  FIREBASE_PROJECT_ID: "group-picks"
  FIREBASE_DATABASE_URL: "https://group-picks-default-rtdb.firebaseio.com"

  # Sentry — slugs and public DSN (NEXT_PUBLIC_SENTRY_DSN is bundled into client JS; not a secret)
  # SENTRY_DSN (server-side) and SENTRY_AUTH_TOKEN are managed as Vercel secrets — not committed here
  SENTRY_ORG: "reedmartz"
  SENTRY_PROJECT: "group-picks"
  NEXT_PUBLIC_SENTRY_DSN: "https://afdaac1d008d17a140375eb11d8ef9b3@o4511071618072576.ingest.us.sentry.io/4511351012851712"
